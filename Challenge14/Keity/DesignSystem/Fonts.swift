//
//  Fonts.swift
//  Challenge14
//
//  Created by Keitiely Silva Viana on 16/05/26.
//
import UIKit

enum AppFonts {
    static let trackTitle  = UIFont.preferredFont(forTextStyle: .headline)
    static let trackArtist = UIFont.preferredFont(forTextStyle: .subheadline)
    static let albumName   = UIFont.preferredFont(forTextStyle: .footnote)
    static let lyrics      = UIFont.preferredFont(forTextStyle: .subheadline)
    static let bluetooth   = UIFont.preferredFont(forTextStyle: .subheadline)
}
